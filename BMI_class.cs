class BMI //Class yang berisikan perhitungan BMI
{
    private float weight;
    private float height;

    public BMI(float weight, float height)
    {
        weight = BB;
        height = TB;
    }
    
    public calcBMI(float weight, float height)
    {
        BMI = (weight*703) / (height*height);
        return BMI;
    }
}
